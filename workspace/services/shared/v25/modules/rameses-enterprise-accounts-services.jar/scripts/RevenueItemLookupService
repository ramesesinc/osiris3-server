import com.rameses.annotations.*;
import com.rameses.util.*;
import java.rmi.server.*;
import com.rameses.common.*
import com.rameses.services.extended.*

class RevenueItemLookupService  {

	@PersistenceContext("main")
	def em;

	@ProxyMethod
	public def getColumns( def o ) {
		return[
			[name:'code', caption:'Item Code' ],
			[name:'title', caption:'Item Title' ]
		];
	}
	
	@ProxyMethod
	public def getList( Map m ) {
		if(m.searchtext) m.code = m.searchtext + "%";
		return em.sqlContext.createNamedQuery("revenueitem:getLookup").setParameters(m).setMaxResults(50).resultList;
	}
}
