def svc = new osiris3.groovy.tester.Service(
    ["app.context" : "etracs25", 
     "app.host": "localhost:8070",
     "app.cluster": "osiris3"] );

def ts = svc.lookup("AFStockUtilService");

def m = [af:'51'];
m.endseries = 5000000;
m.qty = 3;
m.startstub = 1;

def z = ts.getCompletedInfo(m);
z.each { k,v->
    println k + "=" + v;
}



def g = [:];
g.startseries = 6000001; g.xendseries = 6000500; g.af = '51'
ts.validateSeries(g );